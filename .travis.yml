language: bash
sudo: required
services:
  - docker
env:
  global:
    - REGISTRY_USER=serlevki
    - REGISTRY_PASS=gangik228
    - secret: "<thisissecret>"

script:
  - docker-compose up --tag serlevki/myawesome team 
  

after_script:
  - docker images
  
before_deploy:
  - docker login -u "$REGISTRY_USER" -p "$REGISTRY_PASS"

deploy:
  skip_cleanup: true
  provider: script
  script: docker push serlevki/myawesometeam
  on:
    branch: master


